<?xml version="1.0" encoding="utf-8"?>
<!--
    FileProvider paths for sharing the run/bug log.
    Exposes <filesDir>/vlm_runlog.txt via content:// URI.
-->
<paths>
    <files-path name="log_files" path="." />
</paths>
